// retrives the matches of string against a regular expressions
// /match(regex)
// g - global searches (all time accessing)

let text="the rain in spain stays mainly in the plain";
console.log(text.match(/ain/g));
