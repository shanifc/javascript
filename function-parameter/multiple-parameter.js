// multiple parameters is a way to pass more than one parameter to a function.

function greeting(name,age,grade){
    console.log(`name:${name}, age:${age}, grade:${grade}`);

    
}
greeting("shanif",20,"A")